package com.lgmrszd.anshar.transport;

public interface IEmbeddedAudio {
    // empty class used for filtering allowed sounds while in embedded space.
    // only sound instances implementing this interface successfully play when using the client's soundmanager.
    // see SoundSystemMixin for more info.
}
